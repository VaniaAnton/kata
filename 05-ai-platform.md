# AI Platform

<!-- TODO(johny): model gateway, capability contract, provider indirection — see ADR-004 -->

## Capability contract

<!-- TODO(johny): capability → versioned {model + prompt + eval} bundle -->

## Provider indirection & budgets

<!-- TODO(johny): per-capability budgets, tiered routing, fallback chain, named secondary provider -->

## Diagram — AI capability pipeline

<!--
TODO(johny): Mermaid diagram #5: gateway → registry → eval →
promotion candidate → shadow → production.
-->

```mermaid
flowchart LR
  %% TODO(johny)
```
